global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: devops-tasks-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - devops-tasks-api:3000
